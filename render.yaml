services:
  - type: web
    name: unified-marine-coral-species-classifier
    env: python
    runtime: python-3.10.12
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
